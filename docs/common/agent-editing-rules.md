# Правила внесения изменений в код

## Перед применением

- Всегда показывай план и получай подтверждение пользователя.
- Показывай diff перед применением изменений.
- Не применяй изменения автоматически без явного согласия.

## Во время правок

- Добавляй русские комментарии для сложной логики.
- Объясняй внесённые изменения на русском языке.
- Сохраняй английские названия переменных и функций.
- Следуй принципам чистого кода.

## 🚨 Сохранение комментариев

- Никогда не удаляй существующие комментарии, если код остаётся.
- Всегда сохраняй все существующие комментарии при редактировании.
- Добавляй новые комментарии для нового кода.
- Обновляй комментарии только если они относятся к изменяемому коду.
- Не удаляй комментарии даже если кажутся неактуальными.
- Не изменяй комментарии без явного запроса пользователя.
- Не «упрощай» код за счёт удаления комментариев.

### Примеры корректного поведения

```typescript
// Существующий комментарий - СОХРАНИТЬ!
function oldFunction() {
	// Важная логика - СОХРАНИТЬ!
	return data;
}

// При добавлении новой функции:
function oldFunction() {
	// Важная логика - СОХРАНИТЬ!
	return data;
}

// Новый комментарий для новой функции
function newFunction() {
	// Объяснение новой логики
	return newData;
}
```

**Исключения, когда можно удалить комментарий:**

- Комментарий относится к удаляемому коду.
- Пользователь явно попросил удалить конкретный комментарий.
- Комментарий содержит устаревшую информацию об изменяемом коде.
