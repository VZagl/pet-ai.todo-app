# CREATIVE PHASE: Theming UI/UX (theming-001)

> **Тип:** UI/UX Design  
> **Задача:** Двухслойная система тематизации — UI компоненты  
> **Дата:** 2026-02-18

---

## 🎨🎨🎨 ENTERING CREATIVE PHASE: UI/UX Design 🎨🎨🎨

**Focus:** Дизайн UI переключателей темы и модального окна настроек  
**Objective:** Определить структуру header, формат ThemeToggle, SettingsModal, ThemeSelectionTab  
**Requirements:** ThemeProvider с самого начала, overlay блокирует события, контейнер кнопок с flex

---

## 0. Style Guide Check

✅ **Style Guide Available:** `memory-bank/style-guide.md`  
Все визуальные решения должны соответствовать style-guide: минимализм, usability, accessibility, consistency.

---

## 1. PROBLEM STATEMENT

- Требуется переключатель светлая/тёмная тема и кнопка настроек в header
- Настройки тем должны быть в модальном окне с табами
- Селектор тем — мини-карточки с превью light/dark
- Индикатор выбранной темы — визуальное выделение выбранного варианта (светлый/тёмный)
- Цветовые палитры тем — обсуждаются отдельно

---

## 2. DECISIONS

### 2.1. ThemeProvider

**Решение:** ThemeProvider реализуется сразу, с самого начала. Не «пока без ThemeProvider».

---

### 2.2. Контейнер кнопок в header

**Решение:** Создать отдельный контейнер `HeaderControls` (или аналог), который позиционируется (position: absolute, top-right). Внутри — flex для выравнивания кнопок.

**Порядок кнопок (слева направо):**

1. **ThemeToggle** — переключатель светлая/тёмная
2. **LanguageSwitcher** — переключатель языка (существующий)
3. **SettingsButton** — кнопка открытия настроек

LanguageSwitcher остаётся отдельной кнопкой, но входит в общий контейнер. Кнопки выравниваются с помощью flex (gap между ними).

---

### 2.3. ThemeToggle

**Решение:**

- Одна кнопка с иконкой: **sun** в светлой теме, **moon** в тёмной
- Иконки — отдельные SVG-файлы в `src/assets/` (sun.svg, moon.svg)
- Цвет кнопки/иконки меняется при переключении (акцент при активном состоянии)
- Использует ThemeProvider / useTheme

---

### 2.4. SettingsButton

**Решение:**

- Кнопка с иконкой шестерёнки (settings.svg) в `src/assets/`
- По клику открывает модальное окно настроек

---

### 2.5. SettingsModal — структура и overlay

**Решение:**

- Модальное окно размером как `todo-app` (покрывает весь контейнер `.todo-app`)
- **Под окном** — непрозрачная или сильно размытая полупрозрачная подложка (overlay)
- Overlay **блокирует передачу событий** другим элементам (pointer-events, клики не проходят сквозь overlay)
- В правом верхнем углу — кнопка закрытия (иконка close.svg)

---

### 2.6. SettingsModal — подтверждение при закрытии

**Решение:** При закрытии, если есть **неподтверждённые** изменения настроек:

- Показать сообщение: «Настройки не были подтверждены»
- Варианты: **Выйти с сохранением изменений** | **Выйти без сохранения**
- Блокировать закрытие до выбора пользователя

---

### 2.7. SettingsModal — табы

**Решение:**

- Табы настроек (на текущий момент — один таб: «Выбор темы»)
- Структура позволяет добавить табы в будущем

---

### 2.8. ThemeSelectionTab — список тем

**Решение:**

- Список тем в виде **мини-карточек** с gap между ними
- Каждая тема — отдельная карточка выбора
- Темы: Default, Ocean, Forest, Sunset, Lavender (названия не локализуются)

---

### 2.9. ThemeSelectionTab — карточка темы

**Структура карточки:**

- **Сверху:** название темы
- **Слева:** индикатор выбора (или альтернатива)
- **Две мини-карточки:** светлая и тёмная версия темы (превью)
- **Margin** вокруг каждой мини-карточки — чтобы элементы не сливались (рекомендуемый размер: 8px)

---

### 2.10. Индикатор выбранной темы

**Решение:**

- Индикатор — **меняется картинка** выбранного светлый/тёмный + **цвет фона и картинки**
- Цвета **не для каждой темы** — только **светлая/тёмная в палитре текущей темы**
- Т.е. выделение выбранной мини-карточки (light или dark) использует цвета из палитры текущей выбранной темы (light или dark вариант)

---

### 2.11. ThemeSelectionTab — кнопки внизу

**Решение:**

- Внизу таба две кнопки: **Принять** и **Отказаться**
- «Принять» — применяет выбранные настройки и закрывает модалку
- «Отказаться» — отменяет изменения в draft и закрывает модалку

---

## 3. ASSETS

| Файл                      | Назначение                         |
| ------------------------- | ---------------------------------- |
| `src/assets/sun.svg`      | Иконка светлой темы (ThemeToggle)  |
| `src/assets/moon.svg`     | Иконка тёмной темы (ThemeToggle)   |
| `src/assets/settings.svg` | Иконка настроек (SettingsButton)   |
| `src/assets/close.svg`    | Уже есть — кнопка закрытия модалки |

---

## 4. COMPONENT STRUCTURE

```
HeaderControls (контейнер, position, flex)
  ├── ThemeToggle
  ├── LanguageSwitcher
  └── SettingsButton

SettingsModal (модалка на весь todo-app)
  ├── Overlay (подложка, блокирует события)
  ├── ModalWindow (размер как todo-app)
  │     ├── CloseButton (правый верхний угол)
  │     ├── Tabs (табы настроек)
  │     │     └── ThemeSelectionTab
  │     │           ├── Список карточек тем (gap)
  │     │           │     └── ThemeCard: название, индикатор, 2 мини-карточки (light, dark)
  │     │           └── Кнопки: Принять, Отказаться
  │     └── ...
  └── ...
```

---

## 5. IMPLEMENTATION NOTES

- ThemeProvider оборачивает App (или TodoProvider)
- Draft-состояние в SettingsModal: локальные `draftColorScheme`, `draftThemeVariant` до нажатия «Принять»
- Цветовые палитры тем — отдельная creative-фаза (обсуждаются позже)

---

## 6. CREATIVE VERIFICATION CHECKLIST

- [x] Problem clearly defined
- [x] Decisions documented with rationale
- [x] Component structure defined
- [x] Assets listed
- [x] Overlay and event blocking specified
- [x] Theme selection indicator logic clarified

---

## 🎨🎨🎨 EXITING CREATIVE PHASE - DECISIONS MADE 🎨🎨🎨

**Summary:** ThemeToggle (sun/moon) слева, LanguageSwitcher по центру, SettingsButton справа — в общем контейнере с flex. SettingsModal с overlay, табами, подтверждением при несохранённых изменениях. ThemeSelectionTab — мини-карточки тем с индикатором выбора (цвета из палитры текущей темы light/dark).  
**Key Decisions:** HeaderControls container, ThemeProvider сразу, overlay блокирует события, индикатор через цвета текущей темы.  
**Next Steps:** Creative phase «Цветовые палитры» — отдельно. Затем `/build` для реализации.
