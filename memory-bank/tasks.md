# Memory Bank: Tasks

## Current Task

Проверить тесты на ложные совпадения по тексту

## Task ID

test-false-positive-matches-001

## Complexity Level

**Level 1** - Quick Fix

**Обоснование:**

- Изменения только в тестах (2 файла)
- Быстрое выполнение (минуты)
- Низкий риск регрессий
- Изолированное изменение
- Не требует архитектурных решений

**Workflow:** VAN → BUILD → DOCUMENTATION

## Status

🟢 **COMPLETED** - Implementation Complete

- [x] Инициализация Memory Bank
- [x] Определение уровня сложности
- [x] Анализ проблемных мест в тестах
- [x] Реализация исправлений в TodoFooter.test.tsx
- [x] Реализация исправлений в TodoApp.test.tsx
- [x] Тестирование изменений
- [x] Обновление документации (опционально)

## Description

Исправление тестов для исключения ложноположительных результатов при поиске текста. Проверки вида `getByText('3')` или `getByText('осталось')` могут совпадать с текстом в названиях задач (например, "Task 3", "Осталось сделать") и давать ложноположительные результаты.

**Решение:** Использовать `within()` из Testing Library для ограничения области поиска до footer элемента.

## Technology Stack

- Framework: React 19.2.3
- Build Tool: Vite 7.3.0
- Language: TypeScript 5.9.3
- Testing: Vitest 4.0.16 + Testing Library
- Package Manager: pnpm 10.26.2

## Technology Validation

✅ **Технологический стек валидирован** - все зависимости уже установлены и работают в проекте:

- Testing Library с поддержкой `within()` для ограничения области поиска
- Vitest 4.0.16 настроен и используется в других тестах
- React 19.2.3
- TypeScript 5.9.3 для типизации

**Дополнительная валидация не требуется** - задача использует существующий стек без новых зависимостей.

## Requirements

### Функциональные требования (FR)

1. **FR-01**: Исправить все проверки с "голыми" числами в `TodoFooter.test.tsx` (18 проверок)
2. **FR-02**: Исправить все проверки с текстом "осталось" в `TodoFooter.test.tsx`
3. **FR-03**: Исправить проверки счетчика в `TodoApp.test.tsx` (3 проверки)
4. **FR-04**: Использовать `within()` для ограничения области поиска до footer элемента

### Нефункциональные требования (NFR)

1. **NFR-01**: Тесты должны быть более специфичными и надёжными
2. **NFR-02**: Все существующие тесты должны пройти успешно
3. **NFR-03**: Тесты не должны давать ложноположительные результаты

## Files to Modify

### Тестовые файлы:

- `src/components/TodoFooter/TodoFooter.test.tsx` - исправить 18+ проверок с числами и текстом
- `src/components/TodoApp/TodoApp.test.tsx` - исправить 3 проверки счетчика

## Implementation Plan

### Phase 1: Исправление TodoFooter.test.tsx

1. **Импортировать `within` из Testing Library**
   - Добавить `within` в импорты из `@testing-library/react`

2. **Исправить проверки чисел**
   - Найти footer элемент через `screen.getByRole('contentinfo')`
   - Заменить все `screen.getByText('число')` на `within(footer).getByText('число')`
   - Применить ко всем 18 проверкам с числами

3. **Исправить проверки текста "осталось"**
   - Использовать тот же footer элемент
   - Заменить все `screen.getByText('задач осталось')` и аналогичные на `within(footer).getByText('...')`

### Phase 2: Исправление TodoApp.test.tsx

1. **Импортировать `within` из Testing Library**
   - Добавить `within` в импорты из `@testing-library/react`

2. **Исправить тест "должен показывать правильный счетчик активных задач"**
   - Найти footer элемент через `screen.getByRole('contentinfo')` или через поиск после добавления задач
   - Заменить `screen.getByText('3')` на `within(footer).getByText('3')`
   - Заменить `screen.getByText('2')` на `within(footer).getByText('2')`
   - Заменить `screen.getByText(/осталось/i)` на `within(footer).getByText(/осталось/i)`

### Phase 3: Проверка и валидация

1. **Запуск тестов**
   - Запустить все тесты проекта: `pnpm test`
   - Убедиться, что все тесты проходят успешно
   - Проверить, что нет регрессий

2. **Валидация реализации**
   - Убедиться, что тесты более специфичные
   - Убедиться, что тесты не дают ложноположительных результатов
   - Проверить соответствие требованиям FR-01, FR-02, FR-03, FR-04

## Technical Details

### Используемые инструменты

- **Testing Library**: `within()` для ограничения области поиска
- **Testing Library**: `getByRole('contentinfo')` для поиска footer элемента
- **Vitest**: для запуска тестов

### Структура исправления

```typescript
import { render, screen, within } from '@testing-library/react';

// В тесте:
const footer = screen.getByRole('contentinfo');
expect(within(footer).getByText('3')).toBeInTheDocument();
expect(within(footer).getByText('задачи осталось')).toBeInTheDocument();
```

## Success Metrics

- ✅ Все проверки используют `within()` для ограничения области поиска
- ✅ Тесты не дают ложноположительных результатов
- ✅ Все тесты проходят успешно
- ✅ Соответствие требованиям из backlog

## Implementation Results

### Phase 1: Исправление TodoFooter.test.tsx ✅

- ✅ Добавлен импорт `within` из `@testing-library/react`
- ✅ Исправлены все 18 проверок с числами (0, 1, 2, 3, 4, 5, 10, 21, 22, 25, 100, 9999, -1)
- ✅ Исправлены все проверки с текстом "осталось" (задач осталось, задача осталось, задачи осталось)
- ✅ Все проверки используют `within(footer)` для ограничения области поиска до footer элемента
- ✅ Footer элемент получается через `screen.getByRole('contentinfo')`

### Phase 2: Исправление TodoApp.test.tsx ✅

- ✅ Добавлен импорт `within` из `@testing-library/react`
- ✅ Исправлен тест "должен показывать правильный счетчик активных задач"
- ✅ Заменены проверки `getByText('3')`, `getByText('2')`, `getByText(/осталось/i)` на `within(footer).getByText(...)`
- ✅ Footer элемент получается через `screen.getByRole('contentinfo')`

### Phase 3: Проверка и валидация ✅

**Результаты тестирования:**

- ✅ Все тесты проекта: 12 файлов, 116 тестов - все прошли успешно
- ✅ Исправленные тесты проходят успешно
- ✅ Все существующие тесты продолжают проходить
- ✅ Регрессий не обнаружено

**Валидация реализации:**

- ✅ Все проверки используют `within()` для ограничения области поиска
- ✅ Тесты не дают ложноположительных результатов
- ✅ Тесты более специфичные и надёжные
- ✅ Соответствие требованиям FR-01, FR-02, FR-03, FR-04
- ✅ Соответствие требованиям NFR-01, NFR-02, NFR-03

## Summary

Задача успешно завершена. Все тесты исправлены для исключения ложноположительных результатов при поиске текста. Проверки теперь используют `within()` для ограничения области поиска до footer элемента, что делает тесты более специфичными и надёжными.
