# Memory Bank: Tasks

## Current Task

Исправить тест на key в `TodoList`

## Task ID

test-todolist-key-validation-001

## Complexity Level

**Level 1** - Quick Fix

**Обоснование:**

- Изменение одного теста в одном файле
- Быстрое выполнение (минуты)
- Низкий риск регрессий
- Изолированное изменение
- Не требует архитектурных решений

**Workflow:** VAN → BUILD → DOCUMENTATION

## Status

🟡 **IN PROGRESS** - Planning Complete

- [x] Инициализация Memory Bank
- [x] Определение уровня сложности
- [x] Создание ветки `fix/test-todolist-key-validation`
- [x] Планирование реализации (детальный план создан)
- [ ] Реализация изменений в тесте
- [ ] Тестирование изменений
- [ ] Обновление документации

## Description

Исправление теста для проверки использования `id` как `key` в компоненте `TodoList`. Текущая проверка не подтверждает корректное использование `key`. Нужно добавить проверку через перехват `console.error`:

- Сценарий 1: рендер `mockTodos` → ошибок нет
- Сценарий 2: создать массив с дубликатом первого элемента → появляется ошибка о `key`

## Technology Stack

- Framework: React 19.2.3
- Build Tool: Vite 7.3.0
- Language: TypeScript 5.9.3
- Testing: Vitest 4.0.16 + Testing Library
- Package Manager: pnpm 10.26.2

## Technology Validation

✅ **Технологический стек валидирован** - все зависимости уже установлены и работают в проекте:

- Vitest 4.0.16 с поддержкой `vi.spyOn()` для мокирования
- Testing Library настроена и используется в других тестах
- React 19.2.3 с поддержкой предупреждений о `key`
- TypeScript 5.9.3 для типизации

**Дополнительная валидация не требуется** - задача использует существующий стек без новых зависимостей.

## Requirements

### Функциональные требования (FR)

1. **FR-01**: Перехватить `console.error` для проверки предупреждений React о `key`
2. **FR-02**: Проверить отсутствие ошибок при рендере с уникальными `id`
3. **FR-03**: Проверить появление ошибки при рендере с дубликатами `id` в массиве задач

### Нефункциональные требования (NFR)

1. **NFR-01**: Тест должен проверять реальное использование `id` как `key`
2. **NFR-02**: Все существующие тесты должны пройти успешно
3. **NFR-03**: Тест должен обнаруживать проблему при дубликатах `key`

## Files to Modify

### Тестовые файлы:

- `src/components/TodoList/TodoList.test.tsx` - исправить тест на key (строки 69-77)

## Implementation Plan

### Phase 1: Подготовка тестового окружения

1. **Настройка перехвата console.error**
   - Использовать `vi.spyOn(console, 'error')` для перехвата предупреждений React
   - Сохранить оригинальную реализацию для восстановления
   - Очистить spy перед каждым тестом через `beforeEach` или в начале теста

### Phase 2: Реализация тестовых сценариев

2. **Сценарий 1: Проверка отсутствия ошибок при уникальных id**
   - Рендерить компонент `TodoList` с `mockTodos` (все id уникальны)
   - Проверить, что `console.error` не был вызван
   - Проверить, что все элементы отрисовались корректно
   - Убедиться, что количество элементов соответствует `mockTodos.length`

3. **Сценарий 2: Проверка появления ошибки при дубликатах id**
   - Создать массив задач с дубликатом первого элемента:
     ```typescript
     const todosWithDuplicate = [mockTodos[0], ...mockTodos];
     ```
   - Рендерить компонент `TodoList` с массивом, содержащим дубликаты
   - Проверить, что `console.error` был вызван
   - Проверить, что в сообщении об ошибке содержится информация о `key` (например, "key" или "Warning")
   - Проверить, что количество элементов все равно соответствует переданному массиву

### Phase 3: Очистка и восстановление

4. **Восстановление console.error**
   - Использовать `afterEach` или `vi.restoreAllMocks()` для восстановления оригинальной реализации
   - Убедиться, что spy не влияет на другие тесты

### Phase 4: Проверка и валидация

5. **Запуск тестов**
   - Запустить все тесты проекта: `pnpm test`
   - Убедиться, что новый тест проходит успешно
   - Убедиться, что все существующие тесты продолжают проходить
   - Проверить, что нет регрессий

6. **Валидация реализации**
   - Убедиться, что тест действительно проверяет использование `id` как `key`
   - Убедиться, что тест обнаруживает проблему при дубликатах `key`
   - Проверить соответствие требованиям FR-01, FR-02, FR-03

## Technical Details

### Используемые инструменты

- **Vitest**: `vi.spyOn()` для перехвата `console.error`
- **Testing Library**: `render()` для рендеринга компонента
- **React**: Проверка предупреждений о `key` через `console.error`

### Ожидаемое поведение React

- React выдает предупреждение в `console.error`, когда:
  - Элементы в массиве не имеют `key`
  - Элементы в массиве имеют дублирующиеся `key`
  - `key` не является стабильным (меняется между рендерами)

### Структура теста

```typescript
it('должен использовать id задачи как key', () => {
  // Настройка spy
  const consoleErrorSpy = vi.spyOn(console, 'error').mockImplementation(() => {});

  const onToggle = vi.fn();
  const onDelete = vi.fn();

  // Сценарий 1: Уникальные id
  const { rerender } = render(
    <TodoList todos={mockTodos} onToggle={onToggle} onDelete={onDelete} />
  );

  expect(consoleErrorSpy).not.toHaveBeenCalled();
  consoleErrorSpy.mockClear();

  // Сценарий 2: Дубликаты id
  const todosWithDuplicate = [mockTodos[0], ...mockTodos];
  rerender(
    <TodoList todos={todosWithDuplicate} onToggle={onToggle} onDelete={onDelete} />
  );

  expect(consoleErrorSpy).toHaveBeenCalled();
  expect(consoleErrorSpy.mock.calls[0][0]).toContain('key');

  // Восстановление
  consoleErrorSpy.mockRestore();
});
```

## Success Metrics

- ✅ Тест проверяет реальное использование `id` как `key`
- ✅ Тест обнаруживает проблему при дубликатах `key`
- ✅ Все тесты проходят успешно
- ✅ Соответствие требованиям из backlog

## Notes

- Проблема: текущая проверка не подтверждает использование `id` как `key`
- Решение: проверка через перехват `console.error` и тестирование с дубликатами
